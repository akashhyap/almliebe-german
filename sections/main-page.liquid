<main style="padding-top: 4rem;" class="container">
  <div>
    {% if page.title != blank %}
      <h1 class="page-title" style="margin-bottom: 2rem;">
        {{ page.title }}
      </h1>
    {% endif %}
    {% if page.content != blank %}
      <section class="page-content">
        {{ page.content }}
      </section>
    {% endif %}

   
  </div>
</main>
